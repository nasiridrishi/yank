# Common dependencies
zeroconf>=0.131.0  # For mDNS peer discovery
Pillow>=10.0.0     # For image clipboard support

# Windows-specific (install on Windows only)
# pip install pywin32
# pywin32>=306

# macOS-specific (install on macOS only)  
# pip install pyobjc
# pyobjc>=10.0
